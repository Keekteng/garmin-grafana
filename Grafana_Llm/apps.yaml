apiVersion: 1

apps:
  - type: 'grafana-llm-app'
    disabled: false
    jsonData:
      # provider: openai
      openAI:
        url: https://api.openai.com
        organisationId: ${OPENAI_ORGANISATION_KEY}
    secureJsonData:
      openAIKey: ${OPENAI_KEY}
